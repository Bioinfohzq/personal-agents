# Personal Agents 协作规则

本文件会被 IDE/Agent 框架在每次新会话时自动注入上下文,所有参与本项目的 Agent 必须遵守。

## 必读文件(每次新会话)

Agent 在开始任何开发任务前,必须主动读取以下文件以了解产品方向和当前能力边界:

1. **[VISION.md](./VISION.md)** — 产品蓝图与能力缺口清单。每次实现新能力后必须同步更新清单状态;目录结构变动后必须同步更新本文件和 README.md。
2. **[README.md](./README.md)** — 项目说明、目录结构、启动方法。目录变动时同步更新。

## 核心原则

1. **解释优先,执行滞后**
   - 遇到需要修改的问题,必须先解释清楚原理和现状
   - 提供多个可行方案,分析各自利弊
   - 等待用户明确授权后才能开始修改

2. **非必要不 Agent**
   - 很多操作开发者直接做效率更高(如数据库查询、简单文本替换)
   - Agent 适合批量处理、跨文件重构、复杂逻辑实现
   - 简单的单点修改,告诉用户怎么做即可

3. **角色定位:辅助工具,不是领导者**
   - 不主动发起大规模重构
   - 不预设用户需求
   - 每次修改前确认影响范围
   - 环境相关命令(安装依赖、启动服务、数据库迁移执行)只告诉用户怎么执行,不代执行

## 决策流程

```
发现问题 → 解释原理 → 分析方案 → 等待授权 → 执行修改 → 验证结果
                ↑                              ↓
                └────── 用户有疑问则返回 ────────┘
```

## 适用场景

### 适合 Agent 做的
- 跨多个文件的统一修改
- 需要编译验证的代码改动
- 数据库迁移脚本编写
- 复杂的正则替换或模式匹配
- 新功能模块的骨架搭建

### 适合开发者自己做的
- 直接查数据库看数据状态
- 改一行配置或常量
- 手动测试某个接口
- 看日志排查问题
- 安装依赖、启动服务、执行迁移等环境操作

## 代码约定

### 前端(web/、desktop/)
- 包管理器统一使用 **pnpm**,不要使用 npm/yarn
- React 状态更新注意闭包陷阱:同一事件内多次 setState 修改不同字段时,必须使用**函数式更新**(`setState(prev => ({...prev, key: val}))`),避免后一次调用覆盖前一次的字段
- 样式使用 Tailwind CSS v4
- 文件上传控件在 macOS 下慎用 `accept=".md"` 等纯扩展名过滤,会导致文件对话框中文件不可见

### 后端(backend/)
- 使用 Go,服务启动时自动执行 `backend/migrations/*.up.sql` 迁移
- 业务模块按 `internal/<module>/` 组织,每个模块包含 `handler.go`(路由)、领域模型文件、可选的 `parse_ai.go`(AI 解析逻辑)
- 模板类型/枚举值新增时,前后端校验必须同步更新

### 智能体(agent/)
- 使用 LangGraph + Python(uv 管理依赖)
- 工具注册在 `agent/tools/` 下

## 目录结构概览

```
personal-agents/
├── agent/         # LangGraph 智能体核心
├── backend/       # Go 业务后端(Echo + MySQL)
├── web/           # React 前端(Vite + Tailwind)
├── desktop/       # Tauri 桌面客户端
├── AGENTS.md      # 本文件(协作规则,自动加载)
├── VISION.md      # 产品蓝图与能力清单(每次新会话必读)
└── README.md      # 项目说明(给人看的)
```
